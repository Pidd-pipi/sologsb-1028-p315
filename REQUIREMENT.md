# sologsb-1028 · 组件规范与无障碍说明编辑台

> 端口：宿主 `10028` → 容器 `80`

## 原始需求

我需要一个组件规范与无障碍说明编辑台，技术栈用Lit+TypeScript+Spectrum Web Components。维护者可以为每个组件编写用途、属性、状态、键盘行为、读屏说明、禁用场景和示例，并在编辑时切换不同主题和密度预览。组件被删除属性或修改交互后，系统要提示关联示例和说明失效，并允许把旧版本迁移到新版本。需要支持全文搜索、版本差异、代码示例复制、离线编辑和键盘完成主要流程。
